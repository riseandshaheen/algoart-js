{
  "name": "my-dapp",
  "version": "0.1.0",
  "description": "JS DApp",
  "dependencies": {
    "ethers": "^6.7.1",
    "fabric": "5.3.0",
    "viem": "^2.17.3"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  },
  "scripts": {
    "build": "esbuild ./src/index.mjs --bundle --outfile=dist/index.js --platform=node --target=node20",
    "start": "ROLLUP_HTTP_SERVER_URL=\"http://127.0.0.1:5004\" node src/index.mjs"
  },
  "keywords": [
    "cartesi"
  ],
  "license": "MIT"
}
